# Flatten node_modules into a single relocatable tree (no symlinks into a
# virtual .pnpm store), so the install can be copied verbatim into /nix/store.
node-linker=hoisted

# vite-plus is published on the regular npm registry; do not let any ambient
# "wait N days before installing a release" policy hide the pinned version.
min-release-age=0
